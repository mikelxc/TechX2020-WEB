import { StyledComponentProps, Omit, StandardProps, PaletteType, Color, PropTypes } from './Types';

export {
    StyledComponentProps,
    Omit,
    StandardProps, 
    PaletteType,
    Color,
    PropTypes
}